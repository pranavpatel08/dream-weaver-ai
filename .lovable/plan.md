Create a `README.md` at the project root containing the Dream Builder multi-agent platform plan (vision, agent team, dashboard, pages, design, technical approach, phase 1 scope).

Leave the rest of the project as-is — the existing placeholder index page stays untouched. No dependencies, routes, or features added yet.

Once approved, the next prompt can kick off Phase 1 implementation (auth, Lovable Cloud, Firecrawl, agents).